'use strict';

/**
 * passenger-profile controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::passenger-profile.passenger-profile');
